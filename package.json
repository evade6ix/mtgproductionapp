{
  "name": "mtgproductionapp",
  "version": "1.0.0",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd backend && uvicorn app.main:app --host=0.0.0.0 --port=8000"
  }
}
